<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema del Splash Screen -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- El color de fondo del splash screen -->
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <!-- El ícono que se mostrará (tu ícono de la app) -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <!-- El tema al que se cambiará una vez que el splash termine -->
        <item name="postSplashScreenTheme">@style/Theme.MovieNow</item>
    </style>

    <!-- Base application theme. -->
    <style name="Base.Theme.MovieNow" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores primarios de la marca -->
        <item name="colorPrimary">@color/md_theme_primary</item>
        <item name="colorOnPrimary">@color/md_theme_onPrimary</item>
        <item name="colorSecondary">@color/md_theme_secondary</item>
        <item name="colorOnSecondary">@color/md_theme_onSecondary</item>
        <item name="colorTertiary">@color/md_theme_tertiary</item>
        <item name="colorOnTertiary">@color/md_theme_onTertiary</item>

        <!-- Otros colores de la app -->
        <item name="android:colorBackground">@color/md_theme_background</item>
        <item name="colorOnBackground">@color/md_theme_onBackground</item>
        <item name="colorSurface">@color/md_theme_surface</item>
        <item name="colorOnSurface">@color/md_theme_onSurface</item>
    </style>

    <style name="Theme.MovieNow" parent="Base.Theme.MovieNow" />
</resources>
